# SPC-Pile-Flexural-Capacity-Using-ML
This repository provides a Graphical User Interface (GUI) to determine the flexural capacity of SPC pile specimens using the XGBoost machine learning (ML) algorithm.

Download the zipped file to get the code with the necessary media and database.
