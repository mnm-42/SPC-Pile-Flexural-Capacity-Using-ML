# SPC-Pile-Flexural-Capacity-Using-ML
This repository provides a Graphical User Interface (GUI) to determine flexural capacity of SPC pile specimens using XGBoost machine learning (ML) algorithm.
Download the zipped file to get the code with necessary media and database.
